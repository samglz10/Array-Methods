//findIndex = returns the first element that satisfies the testing function

const array1 = [5, 12, 8, 130, 44];
/** 
const isLargeNumber = (element) => {
    if (element > 13)
    return true;
};


console.log(array1.findIndex(isLargeNumber));
*/


const isSmallerNumber = (element) => {

    if (element < 13){
        return true;
    }
    for(let i = 0; i < array.length; i++){
    
    }
    return false;
}
isSmallerNumber([5, 12, 8, 130, 44],5)
